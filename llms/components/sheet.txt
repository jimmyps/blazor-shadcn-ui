# Sheet Component

Side panel that slides in from viewport edges with overlay and animations.

---

## Overview

The Sheet component displays a side panel that slides in from the top, right, bottom, or left edge of the viewport. Features smooth animations, backdrop overlay, and focus management.

**Features:**
- 4 positions (top, right, bottom, left)
- Smooth slide animations
- Backdrop overlay
- Focus trap
- Escape key to close
- Portal rendering
- ARIA attributes
- Dark mode compatible

---

## Package

- **Package:** BlazorUI.Components
- **Namespace:** BlazorUI.Components.Sheet
- **Source:** https://github.com/blazorui-net/ui/tree/main/src/BlazorUI.Components/Components/Sheet

**Note:** Requires `<PortalHost />` in your layout.

---

## API Reference

### Main Components

- `<Sheet>` - Container
- `<SheetTrigger>` - Opens sheet
- `<SheetContent>` - Content panel
- `<SheetHeader>` - Header section
- `<SheetTitle>` - Title
- `<SheetDescription>` - Description
- `<SheetFooter>` - Footer section
- `<SheetClose>` - Close button

---

## Basic Usage

```razor
<Sheet>
    <SheetTrigger AsChild>
        <Button>Open Sheet</Button>
    </SheetTrigger>
    <SheetContent>
        <SheetHeader>
            <SheetTitle>Sheet Title</SheetTitle>
            <SheetDescription>Sheet description</SheetDescription>
        </SheetHeader>
        <p>Sheet content</p>
        <SheetFooter>
            <SheetClose AsChild>
                <Button>Close</Button>
            </SheetClose>
        </SheetFooter>
    </SheetContent>
</Sheet>
```

---

## AsChild Pattern

Use `AsChild` on `SheetTrigger` and `SheetClose` to use your own elements instead of the default `<button>`:

### SheetTrigger with AsChild

```razor
<Sheet>
    <SheetTrigger AsChild>
        <Button Variant="ButtonVariant.Outline">
            <LucideIcon Name="menu" Size="16" />
            Open Menu
        </Button>
    </SheetTrigger>
    <SheetContent Side="SheetSide.Left">
        <!-- Navigation menu -->
    </SheetContent>
</Sheet>
```

### SheetClose with AsChild

```razor
<SheetFooter>
    <SheetClose AsChild>
        <Button Variant="ButtonVariant.Outline">Cancel</Button>
    </SheetClose>
    <Button>Save Changes</Button>
</SheetFooter>
```

**Note:** When `AsChild` is true, the child component (Button) automatically receives trigger behavior via `TriggerContext` including click handling, aria-haspopup, aria-expanded, and focus management.

---

## Examples

### Sheet from Right (Default)

```razor
<Sheet>
    <SheetTrigger AsChild>
        <Button>Open Settings</Button>
    </SheetTrigger>
    <SheetContent Side="SheetSide.Right">
        <SheetHeader>
            <SheetTitle>Settings</SheetTitle>
        </SheetHeader>
        <!-- Settings content -->
    </SheetContent>
</Sheet>
```

### Sheet from Bottom

```razor
<Sheet>
    <SheetTrigger AsChild>
        <Button>Open Menu</Button>
    </SheetTrigger>
    <SheetContent Side="SheetSide.Bottom">
        <!-- Mobile menu content -->
    </SheetContent>
</Sheet>
```

---

## Related Components

- **Dialog** - llms/components/dialog.txt
- **Sidebar** - llms/components/sidebar.txt

---

**Last Updated:** 2025-11-16
