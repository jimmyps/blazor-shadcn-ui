@page "/components/context-menu"
@using BlazorUI.Components.ContextMenu
<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/ContextMenuDemo.razor
=======
@using BlazorUI.Components.Separator
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/ContextMenuDemo.razor

<PageTitle>Context Menu Component - BlazorUI</PageTitle>

<div class="space-y-8 max-w-4xl">
    <div>
        <div class="space-y-3">
            <h1 class="text-4xl font-bold tracking-tight">Context Menu Component</h1>
            <p class="text-xl text-muted-foreground">
<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/ContextMenuDemo.razor
                Displays a menu to the user — such as a set of actions or functions — triggered by right-clicking.
=======
                Displays a menu triggered by right-click.
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/ContextMenuDemo.razor
            </p>
        </div>
    </div>

<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/ContextMenuDemo.razor
    <div class="space-y-6">
        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">Default</h2>
                <p class="text-muted-foreground">
                    Right-click the area below to open the context menu.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click here
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent>
                    <ContextMenuItem OnClick="@(() => Console.WriteLine("Back"))">
                        Back
                        <ContextMenuShortcut>⌘[</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem Disabled="true">
                        Forward
                        <ContextMenuShortcut>⌘]</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem>
                        Reload
                        <ContextMenuShortcut>⌘R</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem>
                        Save As...
                        <ContextMenuShortcut>⇧⌘S</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem>Print...</ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Icons</h2>
                <p class="text-muted-foreground">
                    Add Lucide icons to menu items for better visual hierarchy.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click for icon menu
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent Class="w-64">
                    <ContextMenuLabel>Actions</ContextMenuLabel>
                    <ContextMenuSeparator />
                    <ContextMenuItem OnClick="@(() => HandleIconsAction("Edit"))">
                        <LucideIcon Name="pencil" Size="16" Class="mr-2" />
                        Edit
                        <ContextMenuShortcut>⌘E</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem OnClick="@(() => HandleIconsAction("Duplicate"))">
                        <LucideIcon Name="copy" Size="16" Class="mr-2" />
                        Duplicate
                        <ContextMenuShortcut>⌘D</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem OnClick="@(() => HandleIconsAction("Share"))">
                        <LucideIcon Name="share" Size="16" Class="mr-2" />
                        Share
                    </ContextMenuItem>
                    <ContextMenuItem OnClick="@(() => HandleIconsAction("Download"))">
                        <LucideIcon Name="download" Size="16" Class="mr-2" />
                        Download
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem OnClick="@(() => HandleIconsAction("Archive"))">
                        <LucideIcon Name="archive" Size="16" Class="mr-2" />
                        Archive
                    </ContextMenuItem>
                    <ContextMenuItem OnClick="@(() => HandleIconsAction("Delete"))" Class="text-red-600 focus:text-red-600 focus:bg-red-50 dark:focus:bg-red-950">
                        <LucideIcon Name="trash-2" Size="16" Class="mr-2" />
                        Delete
                        <ContextMenuShortcut>⌫</ContextMenuShortcut>
                    </ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>

            @if (!string.IsNullOrEmpty(iconsAction))
            {
                <p class="text-sm">Last action: <strong>@iconsAction</strong></p>
            }
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Labels and Groups</h2>
                <p class="text-muted-foreground">
                    Organize menu items into groups with labels.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click for file actions
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent Class="w-64">
                    <ContextMenuLabel>File Actions</ContextMenuLabel>
                    <ContextMenuSeparator />
                    <ContextMenuItem>
                        New File
                        <ContextMenuShortcut>⌘N</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem>
                        Open
                        <ContextMenuShortcut>⌘O</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuLabel>Edit</ContextMenuLabel>
                    <ContextMenuSeparator />
                    <ContextMenuItem>
                        Cut
                        <ContextMenuShortcut>⌘X</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem>
                        Copy
                        <ContextMenuShortcut>⌘C</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem>
                        Paste
                        <ContextMenuShortcut>⌘V</ContextMenuShortcut>
                    </ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Destructive Item</h2>
                <p class="text-muted-foreground">
                    Highlight dangerous actions with custom styling.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm bg-muted/50">
                        Right click on this card
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent>
                    <ContextMenuItem>
                        Edit
                        <ContextMenuShortcut>⌘E</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem>
                        Duplicate
                        <ContextMenuShortcut>⌘D</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem>Archive</ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem Class="text-red-600 focus:text-red-600 focus:bg-red-50 dark:focus:bg-red-950">
                        Delete
                        <ContextMenuShortcut>⌫</ContextMenuShortcut>
                    </ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Inset Items</h2>
                <p class="text-muted-foreground">
                    Use inset for items without icons to align with other items.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click here
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent>
                    <ContextMenuItem Inset="true">
                        Profile
                        <ContextMenuShortcut>⇧⌘P</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem Inset="true">
                        Billing
                        <ContextMenuShortcut>⌘B</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuItem Inset="true">
                        Settings
                        <ContextMenuShortcut>⌘S</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem Inset="true">
                        Log out
                        <ContextMenuShortcut>⇧⌘Q</ContextMenuShortcut>
                    </ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">On Image</h2>
                <p class="text-muted-foreground">
                    Context menu on an image element.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <img src="https://images.unsplash.com/photo-1588345921523-c2dcdb7f1dcd?w=400&dpr=2&q=80"
                         alt="Photo"
                         class="w-[300px] h-[200px] object-cover rounded-md cursor-default" />
                </ContextMenuTrigger>
                <ContextMenuContent>
                    <ContextMenuItem>Save Image As...</ContextMenuItem>
                    <ContextMenuItem>Copy Image</ContextMenuItem>
                    <ContextMenuItem>Copy Image Address</ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuItem>Open Image in New Tab</ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">Features</h2>
            </div>
            <ul class="list-disc list-inside space-y-2 text-sm">
                <li>Opens on right-click (contextmenu event)</li>
                <li>Positioned at mouse cursor location</li>
                <li>Closes on outside click or Escape key</li>
                <li>Full keyboard navigation (↑/↓, Enter, Escape)</li>
                <li>Support for disabled items</li>
                <li>Keyboard shortcut hints</li>
                <li>Separator and label components</li>
            </ul>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">Keyboard Navigation</h2>
            </div>
            <div class="grid gap-2 text-sm">
                <div class="flex gap-4">
                    <kbd class="px-2 py-1 bg-muted rounded">↑/↓</kbd>
                    <span>Navigate items</span>
                </div>
                <div class="flex gap-4">
                    <kbd class="px-2 py-1 bg-muted rounded">Enter/Space</kbd>
                    <span>Select item</span>
                </div>
                <div class="flex gap-4">
                    <kbd class="px-2 py-1 bg-muted rounded">Escape</kbd>
                    <span>Close menu</span>
                </div>
            </div>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">Installation</h2>
            </div>

            <div class="bg-muted p-4 rounded-md">
                <code class="text-sm">@@using BlazorUI.Components.ContextMenu</code>
            </div>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">Usage</h2>
            </div>

            <div class="bg-muted p-4 rounded-md">
                <pre class="text-sm overflow-x-auto"><code>&lt;ContextMenu&gt;
    &lt;ContextMenuTrigger&gt;
        &lt;div&gt;Right click here&lt;/div&gt;
    &lt;/ContextMenuTrigger&gt;
    &lt;ContextMenuContent&gt;
        &lt;ContextMenuItem OnClick="HandleCopy"&gt;
            Copy
            &lt;ContextMenuShortcut&gt;⌘C&lt;/ContextMenuShortcut&gt;
        &lt;/ContextMenuItem&gt;
        &lt;ContextMenuSeparator /&gt;
        &lt;ContextMenuItem&gt;Delete&lt;/ContextMenuItem&gt;
    &lt;/ContextMenuContent&gt;
&lt;/ContextMenu&gt;</code></pre>
            </div>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Checkbox Items</h2>
                <p class="text-muted-foreground">
                    Toggle preferences directly from the context menu.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click for view options
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent>
                    <ContextMenuLabel>View Options</ContextMenuLabel>
                    <ContextMenuSeparator />
                    <ContextMenuCheckboxItem @bind-Checked="@showHidden">
                        Show Hidden Files
                    </ContextMenuCheckboxItem>
                    <ContextMenuCheckboxItem @bind-Checked="@showExtensions">
                        Show File Extensions
                    </ContextMenuCheckboxItem>
                    <ContextMenuCheckboxItem @bind-Checked="@compactView">
                        Compact View
                    </ContextMenuCheckboxItem>
                </ContextMenuContent>
            </ContextMenu>

            <div class="text-sm space-x-4">
                <span>Hidden: <strong>@(showHidden ? "Shown" : "Hidden")</strong></span>
                <span>Extensions: <strong>@(showExtensions ? "Shown" : "Hidden")</strong></span>
                <span>Compact: <strong>@(compactView ? "Yes" : "No")</strong></span>
            </div>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Radio Items</h2>
                <p class="text-muted-foreground">
                    Select from mutually exclusive options.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click to select sort order
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent>
                    <ContextMenuLabel>Sort By</ContextMenuLabel>
                    <ContextMenuSeparator />
                    <ContextMenuRadioGroup TValue="string" @bind-Value="@sortOrder">
                        <ContextMenuRadioItem TValue="string" Value="@("name")">Name</ContextMenuRadioItem>
                        <ContextMenuRadioItem TValue="string" Value="@("date")">Date Modified</ContextMenuRadioItem>
                        <ContextMenuRadioItem TValue="string" Value="@("size")">Size</ContextMenuRadioItem>
                        <ContextMenuRadioItem TValue="string" Value="@("type")">Type</ContextMenuRadioItem>
                    </ContextMenuRadioGroup>
                </ContextMenuContent>
            </ContextMenu>

            <p class="text-sm">Sort order: <strong>@sortOrder</strong></p>
        </section>

        <section class="space-y-4">
            <div class="space-y-2">
                <h2 class="text-2xl font-semibold">With Submenus</h2>
                <p class="text-muted-foreground">
                    Organize complex options in nested menus.
                </p>
            </div>

            <ContextMenu>
                <ContextMenuTrigger>
                    <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                        Right click for share options
                    </div>
                </ContextMenuTrigger>
                <ContextMenuContent Class="w-64">
                    <ContextMenuItem OnClick="@(() => HandleContextAction("Copy"))">
                        Copy
                        <ContextMenuShortcut>⌘C</ContextMenuShortcut>
                    </ContextMenuItem>
                    <ContextMenuSeparator />
                    <ContextMenuSub>
                        <ContextMenuSubTrigger>Share</ContextMenuSubTrigger>
                        <ContextMenuSubContent>
                            <ContextMenuItem OnClick="@(() => HandleContextAction("Share: Email"))">
                                Email
                            </ContextMenuItem>
                            <ContextMenuItem OnClick="@(() => HandleContextAction("Share: Slack"))">
                                Slack
                            </ContextMenuItem>
                            <ContextMenuItem OnClick="@(() => HandleContextAction("Share: Teams"))">
                                Microsoft Teams
                            </ContextMenuItem>
                            <ContextMenuSeparator />
                            <ContextMenuSub>
                                <ContextMenuSubTrigger>Social</ContextMenuSubTrigger>
                                <ContextMenuSubContent>
                                    <ContextMenuItem OnClick="@(() => HandleContextAction("Share: Twitter"))">Twitter</ContextMenuItem>
                                    <ContextMenuItem OnClick="@(() => HandleContextAction("Share: Facebook"))">Facebook</ContextMenuItem>
                                    <ContextMenuItem OnClick="@(() => HandleContextAction("Share: LinkedIn"))">LinkedIn</ContextMenuItem>
                                </ContextMenuSubContent>
                            </ContextMenuSub>
                        </ContextMenuSubContent>
                    </ContextMenuSub>
                    <ContextMenuSeparator />
                    <ContextMenuItem OnClick="@(() => HandleContextAction("Delete"))" Class="text-red-600 focus:text-red-600 focus:bg-red-50 dark:focus:bg-red-950">
                        Delete
                        <ContextMenuShortcut>⌫</ContextMenuShortcut>
                    </ContextMenuItem>
                </ContextMenuContent>
            </ContextMenu>

            @if (!string.IsNullOrEmpty(contextAction))
            {
                <p class="text-sm">Last action: <strong>@contextAction</strong></p>
            }
        </section>
=======
    <!-- Basic Example -->
    <div class="space-y-4">
        <div class="space-y-2">
            <h2 class="text-2xl font-semibold">Basic Example</h2>
            <p class="text-sm text-muted-foreground">
                Right-click the area below to open the context menu.
            </p>
        </div>
        <ContextMenu>
            <ContextMenuTrigger>
                <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                    Right click here
                </div>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuItem OnClick="@(() => HandleAction("Back"))">
                    Back
                    <ContextMenuShortcut>Ctrl+[</ContextMenuShortcut>
                </ContextMenuItem>
                <ContextMenuItem OnClick="@(() => HandleAction("Forward"))" Disabled="true">
                    Forward
                    <ContextMenuShortcut>Ctrl+]</ContextMenuShortcut>
                </ContextMenuItem>
                <ContextMenuItem OnClick="@(() => HandleAction("Reload"))">
                    Reload
                    <ContextMenuShortcut>Ctrl+R</ContextMenuShortcut>
                </ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuItem OnClick="@(() => HandleAction("Save Page As..."))">
                    Save Page As...
                    <ContextMenuShortcut>Ctrl+S</ContextMenuShortcut>
                </ContextMenuItem>
                <ContextMenuItem OnClick="@(() => HandleAction("Print"))">
                    Print...
                    <ContextMenuShortcut>Ctrl+P</ContextMenuShortcut>
                </ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
        @if (!string.IsNullOrEmpty(_lastAction))
        {
            <p class="text-sm text-muted-foreground">Last action: @_lastAction</p>
        }
    </div>

    <!-- With Labels -->
    <div class="space-y-4">
        <div class="space-y-2">
            <h2 class="text-2xl font-semibold">With Labels and Groups</h2>
            <p class="text-sm text-muted-foreground">
                Context menu with labeled sections.
            </p>
        </div>
        <ContextMenu>
            <ContextMenuTrigger>
                <div class="flex h-[150px] w-[300px] items-center justify-center rounded-md border border-dashed text-sm">
                    Right click here
                </div>
            </ContextMenuTrigger>
            <ContextMenuContent>
                <ContextMenuLabel>Edit</ContextMenuLabel>
                <ContextMenuItem>Cut</ContextMenuItem>
                <ContextMenuItem>Copy</ContextMenuItem>
                <ContextMenuItem>Paste</ContextMenuItem>
                <ContextMenuSeparator />
                <ContextMenuLabel>View</ContextMenuLabel>
                <ContextMenuItem>Zoom In</ContextMenuItem>
                <ContextMenuItem>Zoom Out</ContextMenuItem>
                <ContextMenuItem>Reset Zoom</ContextMenuItem>
            </ContextMenuContent>
        </ContextMenu>
    </div>

    <!-- API Reference -->
    <div class="space-y-4">
        <div class="space-y-2">
            <h2 class="text-2xl font-semibold">API Reference</h2>
        </div>
        <div class="border rounded-lg p-6 space-y-4">
            <div>
                <h3 class="font-semibold mb-2">Components</h3>
                <div class="space-y-2 text-sm">
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenu</code> - Root component</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenuTrigger</code> - Right-click target area</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenuContent</code> - Menu container</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenuItem</code> - Menu item with click handler</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenuLabel</code> - Section label</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenuSeparator</code> - Visual divider</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">ContextMenuShortcut</code> - Keyboard shortcut hint</p>
                </div>
            </div>
            <Separator />
            <div>
                <h3 class="font-mono text-sm font-semibold mb-2">ContextMenuItem Parameters</h3>
                <div class="space-y-2 text-sm">
                    <p><strong>OnClick:</strong> <code class="bg-muted px-1 py-0.5 rounded">EventCallback</code> - Click handler</p>
                    <p><strong>Disabled:</strong> <code class="bg-muted px-1 py-0.5 rounded">bool</code> - Disables the item</p>
                    <p><strong>Inset:</strong> <code class="bg-muted px-1 py-0.5 rounded">bool</code> - Adds left padding</p>
                </div>
            </div>
        </div>
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/ContextMenuDemo.razor
    </div>
</div>

@code {
<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/ContextMenuDemo.razor
    // Checkbox state
    private bool showHidden = false;
    private bool showExtensions = true;
    private bool compactView = false;

    // Radio state
    private string? sortOrder = "name";

    // Action tracking
    private string? iconsAction;
    private string? contextAction;

    private void HandleIconsAction(string action) => iconsAction = action;
    private void HandleContextAction(string action) => contextAction = action;
=======
    private string? _lastAction;

    private void HandleAction(string action)
    {
        _lastAction = action;
    }
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/ContextMenuDemo.razor
}
