@page "/components/menubar"
@using BlazorUI.Components.Menubar
<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/MenubarDemo.razor
=======
@using BlazorUI.Components.Separator
@using BlazorUI.Components.Kbd
@using BlazorUI.Primitives.Services
@inject IKeyboardShortcutService KeyboardShortcuts
@implements IAsyncDisposable
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/MenubarDemo.razor

<PageTitle>Menubar Component - BlazorUI</PageTitle>

<div class="space-y-8 max-w-4xl">
    <div>
        <div class="space-y-3">
            <h1 class="text-4xl font-bold tracking-tight">Menubar Component</h1>
            <p class="text-xl text-muted-foreground">
<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/MenubarDemo.razor
                A visually persistent menu common in desktop applications that provides quick access to a consistent set of commands.
=======
                A visually persistent menu common in desktop applications.
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/MenubarDemo.razor
            </p>
        </div>
    </div>

<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/MenubarDemo.razor
    <!-- Basic Menubar -->
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">Basic Menubar</h2>
        <p class="text-sm text-muted-foreground">
            A menubar with common application menus like File, Edit, and View.
        </p>

        <div class="flex flex-col gap-4">
=======
    <!-- Example with Keyboard Shortcuts -->
    <div class="space-y-4">
        <div class="space-y-2">
            <h2 class="text-2xl font-semibold">Example</h2>
            <p class="text-sm text-muted-foreground">
                A menubar with working keyboard shortcuts. Press shortcuts anywhere on the page - they work even when menus are closed.
            </p>
        </div>

        <div class="flex items-start gap-4">
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/MenubarDemo.razor
            <Menubar>
                <MenubarMenu>
                    <MenubarTrigger>File</MenubarTrigger>
                    <MenubarContent>
<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/MenubarDemo.razor
                        <MenubarItem OnClick="@(() => HandleAction("New Tab"))">
                            New Tab
                            <MenubarShortcut>⌘T</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("New Window"))">
                            New Window
                            <MenubarShortcut>⌘N</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("New Incognito Window"))" Disabled="true">
                            New Incognito Window
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Share"))">
                            Share
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Print"))">
                            Print...
                            <MenubarShortcut>⌘P</MenubarShortcut>
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>

                <MenubarMenu>
                    <MenubarTrigger>Edit</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleAction("Undo"))">
                            Undo
                            <MenubarShortcut>⌘Z</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Redo"))">
                            Redo
                            <MenubarShortcut>⇧⌘Z</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Cut"))">
                            Cut
                            <MenubarShortcut>⌘X</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Copy"))">
                            Copy
                            <MenubarShortcut>⌘C</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Paste"))">
                            Paste
                            <MenubarShortcut>⌘V</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Select All"))">
                            Select All
                            <MenubarShortcut>⌘A</MenubarShortcut>
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>

                <MenubarMenu>
                    <MenubarTrigger>View</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleAction("Always Show Bookmarks Bar"))">
                            Always Show Bookmarks Bar
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Always Show Full URLs"))">
                            Always Show Full URLs
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Reload"))">
                            Reload
                            <MenubarShortcut>⌘R</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Force Reload"))" Disabled="true">
                            Force Reload
                            <MenubarShortcut>⇧⌘R</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Toggle Fullscreen"))">
                            Toggle Fullscreen
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Hide Sidebar"))">
                            Hide Sidebar
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>

                <MenubarMenu>
                    <MenubarTrigger>Profiles</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleAction("Andy"))">
                            Andy
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Benoit"))">
                            Benoit
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleAction("Luis"))">
                            Luis
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Edit Profiles"))">
                            Edit...
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleAction("Add Profile"))">
                            Add Profile...
                        </MenubarItem>
=======
                        <MenubarItem OnClick="@(() => HandleShortcutAction("New Tab"))">
                            New Tab
                            <MenubarShortcut>Ctrl+Shift+T</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("New Window"))">
                            New Window
                            <MenubarShortcut>Ctrl+Shift+N</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Share"))">Share</MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Print"))">
                            Print...
                            <MenubarShortcut>Ctrl+Shift+P</MenubarShortcut>
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>
                <MenubarMenu>
                    <MenubarTrigger>Edit</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Undo"))">
                            Undo
                            <MenubarShortcut>Ctrl+Z</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Redo"))">
                            Redo
                            <MenubarShortcut>Ctrl+Y</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Find"))">
                            Find
                            <MenubarShortcut>Ctrl+F</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Replace"))">
                            Replace
                            <MenubarShortcut>Ctrl+H</MenubarShortcut>
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>
                <MenubarMenu>
                    <MenubarTrigger>View</MenubarTrigger>
                    <MenubarContent>
                        <MenubarLabel>Appearance</MenubarLabel>
                        <MenubarCheckboxItem @bind-Checked="_showStatusBar">Status Bar</MenubarCheckboxItem>
                        <MenubarCheckboxItem @bind-Checked="_showActivityBar">Activity Bar</MenubarCheckboxItem>
                        <MenubarCheckboxItem @bind-Checked="_showSidebar">Sidebar</MenubarCheckboxItem>
                        <MenubarSeparator />
                        <MenubarLabel>Zoom</MenubarLabel>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Zoom In"))">
                            Zoom In
                            <MenubarShortcut>Ctrl+=</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Zoom Out"))">
                            Zoom Out
                            <MenubarShortcut>Ctrl+-</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarCheckboxItem @bind-Checked="_fullScreen">Full Screen</MenubarCheckboxItem>
                    </MenubarContent>
                </MenubarMenu>
                <MenubarMenu>
                    <MenubarTrigger>Help</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleShortcutAction("Documentation"))">
                            Documentation
                            <MenubarShortcut>F1</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleShortcutAction("About"))">About</MenubarItem>
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/MenubarDemo.razor
                    </MenubarContent>
                </MenubarMenu>
            </Menubar>

<<<<<<< HEAD:demo/BlazorUI.Demo.Shared/Pages/Components/MenubarDemo.razor
            @if (!string.IsNullOrEmpty(lastAction))
            {
                <p class="text-sm">Last action: <strong>@lastAction</strong></p>
            }
        </div>
    </section>

    <!-- With Icons -->
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">With Icons</h2>
        <p class="text-sm text-muted-foreground">
            Add Lucide icons to menu items for better visual hierarchy.
        </p>

        <div class="flex flex-col gap-4">
            <Menubar>
                <MenubarMenu>
                    <MenubarTrigger>
                        <LucideIcon Name="file" Size="16" Class="mr-2" />
                        File
                    </MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleIconsAction("New File"))">
                            <LucideIcon Name="file-plus" Size="16" Class="mr-2" />
                            New File
                            <MenubarShortcut>⌘N</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Open"))">
                            <LucideIcon Name="folder-open" Size="16" Class="mr-2" />
                            Open
                            <MenubarShortcut>⌘O</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Save"))">
                            <LucideIcon Name="save" Size="16" Class="mr-2" />
                            Save
                            <MenubarShortcut>⌘S</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleIconsAction("Print"))">
                            <LucideIcon Name="printer" Size="16" Class="mr-2" />
                            Print
                            <MenubarShortcut>⌘P</MenubarShortcut>
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>

                <MenubarMenu>
                    <MenubarTrigger>
                        <LucideIcon Name="pencil" Size="16" Class="mr-2" />
                        Edit
                    </MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Undo"))">
                            <LucideIcon Name="undo" Size="16" Class="mr-2" />
                            Undo
                            <MenubarShortcut>⌘Z</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Redo"))">
                            <LucideIcon Name="redo" Size="16" Class="mr-2" />
                            Redo
                            <MenubarShortcut>⇧⌘Z</MenubarShortcut>
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleIconsAction("Cut"))">
                            <LucideIcon Name="scissors" Size="16" Class="mr-2" />
                            Cut
                            <MenubarShortcut>⌘X</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Copy"))">
                            <LucideIcon Name="copy" Size="16" Class="mr-2" />
                            Copy
                            <MenubarShortcut>⌘C</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Paste"))">
                            <LucideIcon Name="clipboard" Size="16" Class="mr-2" />
                            Paste
                            <MenubarShortcut>⌘V</MenubarShortcut>
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>

                <MenubarMenu>
                    <MenubarTrigger>
                        <LucideIcon Name="settings" Size="16" Class="mr-2" />
                        Settings
                    </MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Preferences"))">
                            <LucideIcon Name="sliders-horizontal" Size="16" Class="mr-2" />
                            Preferences
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleIconsAction("Account"))">
                            <LucideIcon Name="user" Size="16" Class="mr-2" />
                            Account
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleIconsAction("Help"))">
                            <LucideIcon Name="circle-help" Size="16" Class="mr-2" />
                            Help
                        </MenubarItem>
                    </MenubarContent>
                </MenubarMenu>
            </Menubar>

            @if (!string.IsNullOrEmpty(iconsAction))
            {
                <p class="text-sm">Last action: <strong>@iconsAction</strong></p>
            }
        </div>
    </section>

    <!-- With Disabled Menu Items -->
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">With Disabled Items</h2>
        <p class="text-sm text-muted-foreground">
            Menu items can be disabled to indicate unavailable actions.
        </p>

        <div class="flex flex-col gap-4">
            <Menubar>
                <MenubarMenu>
                    <MenubarTrigger>Document</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleDisabledAction("New"))">New</MenubarItem>
                        <MenubarItem OnClick="@(() => HandleDisabledAction("Open"))">Open</MenubarItem>
                        <MenubarItem OnClick="@(() => HandleDisabledAction("Save"))" Disabled="true">Save (Disabled)</MenubarItem>
                        <MenubarSeparator />
                        <MenubarItem OnClick="@(() => HandleDisabledAction("Export"))">Export</MenubarItem>
                        <MenubarItem OnClick="@(() => HandleDisabledAction("Print"))" Disabled="true">Print (Disabled)</MenubarItem>
                    </MenubarContent>
                </MenubarMenu>
            </Menubar>

            @if (!string.IsNullOrEmpty(disabledAction))
            {
                <p class="text-sm">Last action: <strong>@disabledAction</strong></p>
            }
        </div>
    </section>

    <!-- Keyboard Navigation -->
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">Keyboard Navigation</h2>
        <p class="text-sm text-muted-foreground">
            The menubar supports full keyboard navigation. Try using:
        </p>
        <ul class="text-sm text-muted-foreground list-disc list-inside space-y-1">
            <li><kbd class="px-1 py-0.5 bg-muted rounded text-xs">←</kbd> / <kbd class="px-1 py-0.5 bg-muted rounded text-xs">→</kbd> to navigate between menus</li>
            <li><kbd class="px-1 py-0.5 bg-muted rounded text-xs">↓</kbd> to open a menu</li>
            <li><kbd class="px-1 py-0.5 bg-muted rounded text-xs">↑</kbd> / <kbd class="px-1 py-0.5 bg-muted rounded text-xs">↓</kbd> to navigate menu items</li>
            <li><kbd class="px-1 py-0.5 bg-muted rounded text-xs">Enter</kbd> or <kbd class="px-1 py-0.5 bg-muted rounded text-xs">Space</kbd> to activate an item</li>
            <li><kbd class="px-1 py-0.5 bg-muted rounded text-xs">Escape</kbd> to close the menu</li>
        </ul>

        <Menubar>
            <MenubarMenu>
                <MenubarTrigger>First Menu</MenubarTrigger>
                <MenubarContent>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Item 1"))">Item 1</MenubarItem>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Item 2"))">Item 2</MenubarItem>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Item 3"))">Item 3</MenubarItem>
                </MenubarContent>
            </MenubarMenu>

            <MenubarMenu>
                <MenubarTrigger>Second Menu</MenubarTrigger>
                <MenubarContent>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Option A"))">Option A</MenubarItem>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Option B"))">Option B</MenubarItem>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Option C"))">Option C</MenubarItem>
                </MenubarContent>
            </MenubarMenu>

            <MenubarMenu>
                <MenubarTrigger>Third Menu</MenubarTrigger>
                <MenubarContent>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Choice X"))">Choice X</MenubarItem>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Choice Y"))">Choice Y</MenubarItem>
                    <MenubarItem OnClick="@(() => HandleKeyboardAction("Choice Z"))">Choice Z</MenubarItem>
                </MenubarContent>
            </MenubarMenu>
        </Menubar>

        @if (!string.IsNullOrEmpty(keyboardAction))
        {
            <p class="text-sm">Last keyboard action: <strong>@keyboardAction</strong></p>
        }
    </section>

    <!-- Checkbox and Radio Items -->
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">Checkbox and Radio Items</h2>
        <p class="text-sm text-muted-foreground">
            Menu items that toggle state (checkbox) or allow single selection (radio).
        </p>

        <div class="flex flex-col gap-4">
            <Menubar>
                <MenubarMenu>
                    <MenubarTrigger>View</MenubarTrigger>
                    <MenubarContent>
                        <MenubarLabel>Panels</MenubarLabel>
                        <MenubarSeparator />
                        <MenubarCheckboxItem @bind-Checked="@showToolbar">
                            Show Toolbar
                        </MenubarCheckboxItem>
                        <MenubarCheckboxItem @bind-Checked="@showStatusbar">
                            Show Status Bar
                        </MenubarCheckboxItem>
                        <MenubarCheckboxItem @bind-Checked="@showSidebar" Disabled="true">
                            Show Sidebar (Disabled)
                        </MenubarCheckboxItem>
                    </MenubarContent>
                </MenubarMenu>

                <MenubarMenu>
                    <MenubarTrigger>Zoom</MenubarTrigger>
                    <MenubarContent>
                        <MenubarLabel>Zoom Level</MenubarLabel>
                        <MenubarSeparator />
                        <MenubarRadioGroup TValue="string" @bind-Value="@zoomLevel">
                            <MenubarRadioItem TValue="string" Value="@("50%")">50%</MenubarRadioItem>
                            <MenubarRadioItem TValue="string" Value="@("100%")">100%</MenubarRadioItem>
                            <MenubarRadioItem TValue="string" Value="@("150%")">150%</MenubarRadioItem>
                            <MenubarRadioItem TValue="string" Value="@("200%")">200%</MenubarRadioItem>
                        </MenubarRadioGroup>
                    </MenubarContent>
                </MenubarMenu>
            </Menubar>

            <div class="text-sm space-x-4">
                <span>Toolbar: <strong>@(showToolbar ? "Visible" : "Hidden")</strong></span>
                <span>Status Bar: <strong>@(showStatusbar ? "Visible" : "Hidden")</strong></span>
                <span>Zoom: <strong>@zoomLevel</strong></span>
            </div>
        </div>
    </section>

    <!-- Submenus -->
    <section class="space-y-4">
        <h2 class="text-2xl font-semibold">Submenus</h2>
        <p class="text-sm text-muted-foreground">
            Nested menus for organizing complex menu structures.
        </p>

        <div class="flex flex-col gap-4">
            <Menubar>
                <MenubarMenu>
                    <MenubarTrigger>Insert</MenubarTrigger>
                    <MenubarContent>
                        <MenubarItem OnClick="@(() => HandleSubmenuAction("Text"))">
                            Text
                            <MenubarShortcut>⌘T</MenubarShortcut>
                        </MenubarItem>
                        <MenubarItem OnClick="@(() => HandleSubmenuAction("Image"))">
                            Image
                        </MenubarItem>
                        <MenubarSeparator />
                        <MenubarSub>
                            <MenubarSubTrigger>Shape</MenubarSubTrigger>
                            <MenubarSubContent>
                                <MenubarItem OnClick="@(() => HandleSubmenuAction("Rectangle"))">Rectangle</MenubarItem>
                                <MenubarItem OnClick="@(() => HandleSubmenuAction("Circle"))">Circle</MenubarItem>
                                <MenubarItem OnClick="@(() => HandleSubmenuAction("Triangle"))">Triangle</MenubarItem>
                                <MenubarSeparator />
                                <MenubarSub>
                                    <MenubarSubTrigger>Arrows</MenubarSubTrigger>
                                    <MenubarSubContent>
                                        <MenubarItem OnClick="@(() => HandleSubmenuAction("Arrow Right"))">Right Arrow</MenubarItem>
                                        <MenubarItem OnClick="@(() => HandleSubmenuAction("Arrow Left"))">Left Arrow</MenubarItem>
                                        <MenubarItem OnClick="@(() => HandleSubmenuAction("Arrow Up"))">Up Arrow</MenubarItem>
                                        <MenubarItem OnClick="@(() => HandleSubmenuAction("Arrow Down"))">Down Arrow</MenubarItem>
                                    </MenubarSubContent>
                                </MenubarSub>
                            </MenubarSubContent>
                        </MenubarSub>
                        <MenubarSub>
                            <MenubarSubTrigger>Chart</MenubarSubTrigger>
                            <MenubarSubContent>
                                <MenubarItem OnClick="@(() => HandleSubmenuAction("Bar Chart"))">Bar Chart</MenubarItem>
                                <MenubarItem OnClick="@(() => HandleSubmenuAction("Line Chart"))">Line Chart</MenubarItem>
                                <MenubarItem OnClick="@(() => HandleSubmenuAction("Pie Chart"))">Pie Chart</MenubarItem>
                            </MenubarSubContent>
                        </MenubarSub>
                    </MenubarContent>
                </MenubarMenu>
            </Menubar>

            @if (!string.IsNullOrEmpty(submenuAction))
            {
                <p class="text-sm">Last insert action: <strong>@submenuAction</strong></p>
            }
        </div>
    </section>
</div>

@code {
    private string? lastAction;
    private string? iconsAction;
    private string? disabledAction;
    private string? keyboardAction;
    private string? submenuAction;

    // Checkbox state
    private bool showToolbar = true;
    private bool showStatusbar = true;
    private bool showSidebar = false;

    // Radio state
    private string? zoomLevel = "100%";

    private void HandleAction(string action) => lastAction = action;
    private void HandleIconsAction(string action) => iconsAction = action;
    private void HandleDisabledAction(string action) => disabledAction = action;
    private void HandleKeyboardAction(string action) => keyboardAction = action;
    private void HandleSubmenuAction(string action) => submenuAction = action;
=======
            @if (!string.IsNullOrEmpty(_shortcutAction))
            {
                <div class="flex items-center gap-2 p-3 bg-primary/10 text-primary rounded-md animate-pulse">
                    <LucideIcon Name="check-circle" Size="16" />
                    <span>Shortcut triggered: <strong>@_shortcutAction</strong></span>
                </div>
            }
        </div>

        <div class="mt-4 p-4 bg-muted rounded-lg">
            <p class="font-medium mb-3">Available shortcuts:</p>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-2 text-sm">
                <div class="flex items-center gap-2">
                    <Kbd Keys="Ctrl+Shift+T" Size="KbdSize.Small" /> <span class="text-muted-foreground">New Tab</span>
                </div>
                <div class="flex items-center gap-2">
                    <Kbd Keys="Ctrl+Shift+N" Size="KbdSize.Small" /> <span class="text-muted-foreground">New Window</span>
                </div>
                <div class="flex items-center gap-2">
                    <Kbd Keys="Ctrl+Z" Size="KbdSize.Small" /> <span class="text-muted-foreground">Undo</span>
                </div>
                <div class="flex items-center gap-2">
                    <Kbd Keys="Ctrl+Y" Size="KbdSize.Small" /> <span class="text-muted-foreground">Redo</span>
                </div>
                <div class="flex items-center gap-2">
                    <Kbd Keys="Ctrl+F" Size="KbdSize.Small" /> <span class="text-muted-foreground">Find</span>
                </div>
                <div class="flex items-center gap-2">
                    <Kbd Keys="Ctrl+H" Size="KbdSize.Small" /> <span class="text-muted-foreground">Replace</span>
                </div>
                <div class="flex items-center gap-2">
                    <Kbd Keys="F1" Size="KbdSize.Small" /> <span class="text-muted-foreground">Documentation</span>
                </div>
            </div>
        </div>
    </div>

    <!-- API Reference -->
    <div class="space-y-4">
        <div class="space-y-2">
            <h2 class="text-2xl font-semibold">API Reference</h2>
        </div>
        <div class="border rounded-lg p-6 space-y-4">
            <div>
                <h3 class="font-semibold mb-2">Components</h3>
                <div class="space-y-2 text-sm">
                    <p><code class="bg-muted px-1 py-0.5 rounded">Menubar</code> - Root container</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarMenu</code> - Individual menu</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarTrigger</code> - Menu button</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarContent</code> - Dropdown content</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarItem</code> - Menu item</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarCheckboxItem</code> - Toggleable menu item</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarLabel</code> - Section label</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarSeparator</code> - Visual divider</p>
                    <p><code class="bg-muted px-1 py-0.5 rounded">MenubarShortcut</code> - Keyboard shortcut hint</p>
                </div>
            </div>
            <Separator />
            <div>
                <h3 class="font-mono text-sm font-semibold mb-2">MenubarItem Parameters</h3>
                <div class="space-y-2 text-sm">
                    <p><strong>OnClick:</strong> <code class="bg-muted px-1 py-0.5 rounded">EventCallback</code> - Click handler</p>
                    <p><strong>Disabled:</strong> <code class="bg-muted px-1 py-0.5 rounded">bool</code> - Disables the item</p>
                    <p><strong>Inset:</strong> <code class="bg-muted px-1 py-0.5 rounded">bool</code> - Adds left padding</p>
                </div>
            </div>
            <Separator />
            <div>
                <h3 class="font-mono text-sm font-semibold mb-2">MenubarCheckboxItem Parameters</h3>
                <div class="space-y-2 text-sm">
                    <p><strong>Checked:</strong> <code class="bg-muted px-1 py-0.5 rounded">bool</code> - Whether the item is checked</p>
                    <p><strong>CheckedChanged:</strong> <code class="bg-muted px-1 py-0.5 rounded">EventCallback&lt;bool&gt;</code> - Callback when checked state changes</p>
                    <p><strong>Disabled:</strong> <code class="bg-muted px-1 py-0.5 rounded">bool</code> - Disables the item</p>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private string? _shortcutAction;

    // View menu checkbox states
    private bool _showStatusBar = true;
    private bool _showActivityBar = true;
    private bool _showSidebar = false;
    private bool _fullScreen = false;

    // Keyboard shortcut registrations
    private IDisposable? _shortcutNewTab;
    private IDisposable? _shortcutNewWindow;
    private IDisposable? _shortcutPrint;
    private IDisposable? _shortcutUndo;
    private IDisposable? _shortcutRedo;
    private IDisposable? _shortcutFind;
    private IDisposable? _shortcutReplace;
    private IDisposable? _shortcutDocs;
    private CancellationTokenSource? _clearCts;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // Register keyboard shortcuts
            _shortcutNewTab = await KeyboardShortcuts.RegisterAsync("Ctrl+Shift+T", () => HandleShortcutAsync("New Tab"));
            _shortcutNewWindow = await KeyboardShortcuts.RegisterAsync("Ctrl+Shift+N", () => HandleShortcutAsync("New Window"));
            _shortcutPrint = await KeyboardShortcuts.RegisterAsync("Ctrl+Shift+P", () => HandleShortcutAsync("Print"));
            _shortcutUndo = await KeyboardShortcuts.RegisterAsync("Ctrl+Z", () => HandleShortcutAsync("Undo"));
            _shortcutRedo = await KeyboardShortcuts.RegisterAsync("Ctrl+Y", () => HandleShortcutAsync("Redo"));
            _shortcutFind = await KeyboardShortcuts.RegisterAsync("Ctrl+F", () => HandleShortcutAsync("Find"));
            _shortcutReplace = await KeyboardShortcuts.RegisterAsync("Ctrl+H", () => HandleShortcutAsync("Replace"));
            _shortcutDocs = await KeyboardShortcuts.RegisterAsync("F1", () => HandleShortcutAsync("Documentation"));
        }
    }

    private async Task HandleShortcutAsync(string action)
    {
        _shortcutAction = action;
        await InvokeAsync(StateHasChanged);

        // Cancel any previous clear timer
        _clearCts?.Cancel();
        _clearCts = new CancellationTokenSource();

        // Clear after 2 seconds
        try
        {
            await Task.Delay(2000, _clearCts.Token);
            _shortcutAction = null;
            await InvokeAsync(StateHasChanged);
        }
        catch (TaskCanceledException)
        {
            // Timer was cancelled, ignore
        }
    }

    private void HandleShortcutAction(string action)
    {
        _ = HandleShortcutAsync(action);
    }

    public ValueTask DisposeAsync()
    {
        _clearCts?.Cancel();
        _clearCts?.Dispose();
        _shortcutNewTab?.Dispose();
        _shortcutNewWindow?.Dispose();
        _shortcutPrint?.Dispose();
        _shortcutUndo?.Dispose();
        _shortcutRedo?.Dispose();
        _shortcutFind?.Dispose();
        _shortcutReplace?.Dispose();
        _shortcutDocs?.Dispose();
        return ValueTask.CompletedTask;
    }
>>>>>>> 8835bfed9859e4bf8349954ac05f732fe9ffddcf:demo/BlazorUI.Demo/Pages/Components/MenubarDemo.razor
}
