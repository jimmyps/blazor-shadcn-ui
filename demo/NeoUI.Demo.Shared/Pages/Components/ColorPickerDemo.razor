@page "/components/color-picker"
@using System.ComponentModel.DataAnnotations

<PageTitle>Color Picker - NeoUI</PageTitle>

<div class="space-y-8 max-w-4xl">

    <DemoPageHeader
        Title="Color Picker"
        Description="Color selection with multiple format support (Hex, RGB, HSL) and preset color swatches." />

    <DemoSection
        Title="Compact Size (Default)"
        Description="Space-efficient picker with smaller canvas and fewer presets. Ideal for forms.">
        <DemoBlock Code="@_defaultCode">
            <div class="max-w-[160px]">
                <ColorPicker @bind-Color="hexColor" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @hexColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="Large Size"
        Description="Full-featured picker with larger canvas and more presets.">
        <DemoBlock Code="@_largeCode">
            <div class="max-w-[160px]">
                <ColorPicker @bind-Color="largeColor" Size="ColorPickerSize.Large" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @largeColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="Canvas + Presets Only"
        Description="No RGB inputs, just canvas gradient and quick color swatches.">
        <DemoBlock Code="@_presetsOnlyCode">
            <div class="max-w-[160px]">
                <ColorPicker @bind-Color="presetsOnlyColor" ShowInputs="false" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @presetsOnlyColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="Minimal Picker"
        Description="Canvas and sliders only - no RGB inputs or preset swatches.">
        <DemoBlock Code="@_minimalCode">
            <div class="max-w-[160px]">
                <ColorPicker @bind-Color="minimalColor" ShowInputs="false" ShowPresets="false" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @minimalColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="RGB Format"
        Description="Returns color in RGB format: rgb(r, g, b).">
        <DemoBlock Code="@_rgbCode">
            <div class="max-w-[250px]">
                <ColorPicker @bind-Color="rgbColor" Format="ColorFormat.RGB" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @rgbColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="HSL Format"
        Description="Returns color in HSL format: hsl(h, s%, l%).">
        <DemoBlock Code="@_hslCode">
            <div class="max-w-[250px]">
                <ColorPicker @bind-Color="hslColor" Format="ColorFormat.HSL" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @hslColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="With Alpha Channel"
        Description="Includes opacity slider for transparent colors.">
        <DemoBlock Code="@_alphaCode">
            <div class="max-w-[160px]">
                <ColorPicker @bind-Color="alphaColor" ShowAlpha="true" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @alphaColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="RGBA Format"
        Description="RGB with alpha: rgba(r, g, b, a).">
        <DemoBlock Code="@_rgbaCode">
            <div class="max-w-[250px]">
                <ColorPicker @bind-Color="rgbaColor" Format="ColorFormat.RGB" ShowAlpha="true" />
                <p class="text-xs text-muted-foreground mt-2">Selected: @rgbaColor</p>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="Disabled State"
        Description="A disabled color picker that cannot be changed.">
        <DemoBlock Code="@_disabledCode">
            <div class="max-w-[160px]">
                <ColorPicker @bind-Color="disabledColor" Disabled="true" />
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="With Form Validation"
        Description="Color picker integrated with Blazor form validation.">
        <DemoBlock Code="@_formCode">
            <EditForm Model="@formModel" OnValidSubmit="HandleValidSubmit" class="max-w-[160px] space-y-4">
                <DataAnnotationsValidator />
                <Field>
                    <Label For="form-color">Favorite Color</Label>
                    <ColorPicker @bind-Color="formModel.FavoriteColor"
                                 Id="form-color"
                                 Required="true"
                                 ShowValidationError="true"
                                 ValueExpression="@(() => formModel.FavoriteColor)" />
                </Field>
                <div>
                    <button type="submit" class="inline-flex items-center justify-center rounded-md text-sm font-medium bg-primary text-primary-foreground hover:bg-primary/90 h-10 py-2 px-4">
                        Submit
                    </button>
                </div>
                @if (!string.IsNullOrEmpty(submitMessage))
                {
                    <div class="p-4 rounded-md bg-green-50 dark:bg-green-900/20 text-green-900 dark:text-green-100">
                        @submitMessage
                    </div>
                }
            </EditForm>
        </DemoBlock>
    </DemoSection>

    <DemoSection
        Title="All Formats Side by Side"
        Description="Compare different color format outputs with the same visual picker.">
        <DemoBlock Code="@_comparisonCode">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                <div>
                    <h3 class="text-sm font-medium mb-2">Hex</h3>
                    <ColorPicker @bind-Color="comparisonColor" />
                    <p class="text-xs text-muted-foreground mt-1">@comparisonColor</p>
                </div>
                <div>
                    <h3 class="text-sm font-medium mb-2">RGB</h3>
                    <ColorPicker @bind-Color="comparisonColorRgb" Format="ColorFormat.RGB" />
                    <p class="text-xs text-muted-foreground mt-1">@comparisonColorRgb</p>
                </div>
                <div>
                    <h3 class="text-sm font-medium mb-2">HSL</h3>
                    <ColorPicker @bind-Color="comparisonColorHsl" Format="ColorFormat.HSL" />
                    <p class="text-xs text-muted-foreground mt-1">@comparisonColorHsl</p>
                </div>
            </div>
        </DemoBlock>
    </DemoSection>

    <DemoSection Title="API Reference" Description="Component parameters and their types.">
        <DemoPropsTable Rows="_colorPickerProps" />
    </DemoSection>

    <DemoPageNav />
</div>

@code {
    private string? hexColor = "#3B82F6";
    private string? largeColor = "#EC4899";
    private string? minimalColor = "#10B981";
    private string? presetsOnlyColor = "#F59E0B";
    private string? rgbColor = "rgb(239, 68, 68)";
    private string? hslColor = "hsl(142, 71%, 45%)";
    private string? alphaColor = "#8B5CF6AA";
    private string? rgbaColor = "rgba(236, 72, 153, 0.75)";
    private string? disabledColor = "#6B7280";
    private string? comparisonColor = "#EC4899";
    private string? comparisonColorRgb = "rgb(236, 72, 153)";
    private string? comparisonColorHsl = "hsl(330, 81%, 60%)";
    private string? submitMessage;

    private FormModel formModel = new();

    private void HandleValidSubmit()
    {
        submitMessage = $"Form submitted! Selected color: {formModel.FavoriteColor}";
    }

    public class FormModel
    {
        [Required(ErrorMessage = "Please select a color")]
        public string? FavoriteColor { get; set; } = "#EC4899";
    }
}
