@namespace NeoUI.Blazor.Primitives

@*
    SheetDescriptionPrimitive provides the description element for the sheet.
    Used for accessibility (aria-describedby).
*@

<p id="@Context.DescriptionId"
   @attributes="AdditionalAttributes">
    @ChildContent
</p>

@code {
    [CascadingParameter]
    private SheetContext Context { get; set; } = null!;

    /// <summary>
    /// The description text or content.
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    /// <summary>
    /// Additional attributes to apply to the description element.
    /// </summary>
    [Parameter(CaptureUnmatchedValues = true)]
    public Dictionary<string, object>? AdditionalAttributes { get; set; }

    protected override void OnInitialized()
    {
        if (Context == null)
        {
            throw new InvalidOperationException(
                "SheetDescriptionPrimitive must be used within a SheetPrimitive component. " +
                "Ensure SheetDescriptionPrimitive is a child of a SheetPrimitive component.");
        }
    }
}
