@namespace NeoUI.Blazor.Primitives

@* CollapsibleContentPrimitive primitive - collapsible content region *@
@if (_shouldRender)
{
    <div role="region"
         data-state="@(Context?.Open ?? false ? "open" : "closed")"
         aria-hidden="@(!(Context?.Open ?? false))"
         @attributes="AdditionalAttributes">
        @ChildContent
    </div>
}
