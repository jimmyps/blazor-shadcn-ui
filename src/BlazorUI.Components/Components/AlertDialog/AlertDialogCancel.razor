@namespace BlazorUI.Components.AlertDialog
@using BlazorUI.Primitives.Dialog

@*
    AlertDialog cancel button - closes the dialog.
*@

<DialogClose AsChild="@AsChild">
    @ChildContent
</DialogClose>

@code {
    /// <summary>
    /// The content to render as the cancel button.
    /// </summary>
    [Parameter]
    public RenderFragment? ChildContent { get; set; }

    /// <summary>
    /// When true, the button will be rendered as a child element.
    /// </summary>
    [Parameter]
    public bool AsChild { get; set; }
}
