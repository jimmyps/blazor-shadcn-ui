namespace BlazorUI.Components.Chart;

/// <summary>
/// Represents plot padding (top, right, bottom, left).
/// </summary>
public readonly record struct Padding(int Top, int Right, int Bottom, int Left);
