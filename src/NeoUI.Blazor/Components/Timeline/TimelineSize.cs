namespace NeoUI.Blazor;

/// <summary>
/// Defines the size variant for Timeline components.
/// </summary>
public enum TimelineSize
{
    /// <summary>Small size with compact spacing (gap-2).</summary>
    Small,
    /// <summary>Medium size with moderate spacing (gap-4). Default.</summary>
    Medium,
    /// <summary>Large size with generous spacing (gap-6).</summary>
    Large
}
