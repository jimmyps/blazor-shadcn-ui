namespace NeoUI.Blazor;

/// <summary>Controls the height of a <see cref="FilterChip"/>.</summary>
public enum FilterChipSize
{
    /// <summary>28 px — compact rows and dense UIs (default).</summary>
    Small,

    /// <summary>32 px — standard inline filter bar.</summary>
    Medium,

    /// <summary>36 px — prominent filter toolbars.</summary>
    Large,
}
